<Page x:Class="ToDoList.WorkTasksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ToDoList"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WorkTasksPage">

    <Grid>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
            <TextBlock Text="Twoje zadania:"/>
            <ItemsControl ItemsSource="{Binding WorkTaskList}" Margin="10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:WorkTask>
                 
                        </local:WorkTask>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="Podaj tytuł zadania:"/>
            <TextBox Text="{Binding newWorkTaskTitle}"></TextBox>
            <TextBlock Text="Podaj treść zadania:"/>
            <TextBox Text="{Binding newWorkTaskDescription}"></TextBox>
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding AddNewTaskCommand}" Content="Dodaj zadanie"></Button>
                <Button Content="Usuń zadanie"></Button>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</Page>
